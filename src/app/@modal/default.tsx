/**
 * The modal slot renders nothing unless a project route has been
 * intercepted. Next.js requires this file for every parallel route.
 */
export default function ModalDefault() {
  return null;
}
